# Optimize log — `Hackathon/Graph/Walks.lean`

**Date:** 2026-05-10 14:09:26  
**Backend:** Claude (Anthropic)  

---

**Baseline:** 2.69s  
**Blocks:** 5 total — 5 optimisable, 0 sorry, 0 error  

---

## [1] `example (v : V) : (Walk.nil : G.Walk v v).reverse = Walk.nil := by`

**Score:** 12.27 → 0.27  
**Time:** 2.69s → 2.71s (-1.0%)  

**Before:**
```lean
-- HINT: `simp` with `Walk.reverse_nil`.
    simp[Walk.reverse_nil]
```

**After:**
```lean
exact Walk.reverse_nil
```

**Why chosen:** best quality score (0.27).  
**Alternatives considered:**  
- `  simp only [Walk.reverse_nil]` — score 7.77 (+7.50 vs chosen), t=2.75s (-0.04s vs chosen)  
- `  rfl` — score 1.77 (+1.50 vs chosen), t=2.72s (-0.01s vs chosen)  

---

## [2] `example {u v : V} (p : G.Walk u v) : p.reverse.length = p.length := by`

**Score:** 6.27 → 0.27  
**Time:** 2.69s → 2.69s (+0.9%)  

**Before:**
```lean
-- HINT: `Walk.length_reverse`.
    simp[Walk.length_reverse]
```

**After:**
```lean
rw [Walk.length_reverse]
```

**Why chosen:** best quality score (0.27).  
**Alternatives considered:**  
- `  exact Walk.length_reverse p` — score 0.30 (+0.03 vs chosen), t=2.98s (-0.29s vs chosen)  
- `  simp only [Walk.length_reverse]` — score 7.78 (+7.51 vs chosen), t=2.76s (-0.07s vs chosen)  

---

## [3] `example {u v : V} (p : G.Walk u v) : (p.append Walk.nil).length = p.length := by`

**Score:** 18.27 → 0.28  
**Time:** 2.69s → 2.78s (-3.3%)  

**Before:**
```lean
-- HINT: induction on `p`, or use `Walk.append_nil` + `simp`.
  induction p with
  | nil => simp
  | cons h p ih => simp
```

**After:**
```lean
rw [Walk.append_nil]
```

**Why chosen:** best quality score (0.28).  
**Alternatives considered:**  
- `-- HINT: induction on `p`, or use `Walk.append_nil` + `simp`` — score 12.28 (+12.00 vs chosen), t=2.82s (-0.04s vs chosen)  
- `simp only [Walk.append_nil]` — score 7.76 (+7.49 vs chosen), t=2.65s (+0.13s vs chosen)  

---

## [4] `example {u v : V} (p : G.Walk u v) (h : p.length = 0) : u = v := by`

**Score:** 6.28 → 6.28  
**Time:** 2.69s → 2.75s (+0.9%)  

**Before:**
```lean
cases p
  · rfl
  · simp at h
```

**After:**
```lean
cases p with
  | nil => rfl
  | cons h_edge rest => simp [Walk.length_cons] at h
```

**Why chosen:** best quality score (6.28).  
**Alternatives considered:**  
- `cases p · rfl · simp [Walk.length_cons] at h` — score 6.28 (+0.00 vs chosen), t=2.77s (-0.02s vs chosen)  
- `cases p with   | nil => rfl   | cons hadj tail =>       simp` — score 6.28 (+0.00 vs chosen), t=2.78s (-0.03s vs chosen)  

---

## [5] `example {u v : V} (p : G.Walk u v) (hp : p.IsPath) : p.reverse.IsPath := by`

**Score:** -1.22 → -2.73  
**Time:** 2.69s → 2.73s (+0.8%)  

**Before:**
```lean
-- HINT: `Walk.IsPath.reverse`.
  exact hp.reverse
```

**After:**
```lean
apply Walk.IsPath.reverse
  exact hp
```

**Why chosen:** best quality score (-2.73).  
**Alternatives considered:**  
- `-- HINT: `Walk.IsPath.reverse`.   exact hp.reverse` — score -1.23 (+1.50 vs chosen), t=2.73s (+0.00s vs chosen)  
- `  exact Walk.IsPath.reverse hp` — score 0.27 (+3.00 vs chosen), t=2.70s (+0.03s vs chosen)  

---

## Summary

| | |
|---|---|
| Blocks reviewed | 5 |
| Blocks improved | 5 |
| Baseline runtime | 2.69s |
| Final runtime | 2.73s |
| Time saved | -0.04s (-1.6% faster) |
